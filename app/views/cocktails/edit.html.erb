<h1> Edit The <%= @cocktail.name %></h1>
<%= form_for(@cocktail) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.label :price %>
    <%= f.number_field :price, step: :any %>
    <br>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <br>
    <%= f.label :bars %>
    <%= f.collection_check_boxes :bar_ids, Bar.all, :id, :name %> <!-- this is some magic, idk -->
    <!-- #this is basically gonna be a mix of new, edit, & destroy bar_cocktail all at the same time. should we make a method for it inside of cocktail.rb? not sure... -->
    <br>
    <%= f.submit("Submit Changes") %>
<% end %>